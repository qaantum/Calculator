<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ciphio">
        <activity
            android:name="com.ciphio.vault.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Ciphio"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ciphiovault" />
            </intent-filter>
        </activity>
        
        <!-- Autofill Service -->
        <service
            android:name="com.ciphio.vault.autofill.CiphioAutofillService"
            android:permission="android.permission.BIND_AUTOFILL_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.autofill.AutofillService" />
            </intent-filter>
            <meta-data
                android:name="android.autofill"
                android:resource="@xml/autofill_service" />
        </service>
        
        <!-- Autofill Authentication Activity -->
        <activity
            android:name="com.ciphio.vault.autofill.AutofillAuthActivity"
            android:exported="false"
            android:theme="@style/Theme.Ciphio"
            android:taskAffinity=""
            android:excludeFromRecents="true" />
        
        <!-- Autofill Selection Activity -->
        <activity
            android:name="com.ciphio.vault.autofill.AutofillSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.Ciphio"
            android:taskAffinity=""
            android:excludeFromRecents="true" />
        
        <!-- Autofill Broadcast Receiver -->
        <receiver
            android:name="com.ciphio.vault.autofill.AutofillBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.ciphio.vault.autofill.CREDENTIALS_READY" />
            </intent-filter>
        </receiver>
    </application>

</manifest>

